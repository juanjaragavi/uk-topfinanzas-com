fix(quiz): disable redirect when cookie validation is off

Skip quiz-to-recommender redirect when validation is disabled, allowing repeat form submissions for testing. This matches the existing cookie persistence guard behavior.

## Changes
- Updated `/app/quiz/page.tsx` to check `NEXT_PUBLIC_COOKIE_VALIDATION_ENABLED` before redirecting
- Updated `/app/quiz-2/page.tsx` with the same validation check
- Ensures users can resubmit forms when validation is disabled
- Maintains redirect functionality when validation is enabled

## Behavior
- When `NEXT_PUBLIC_COOKIE_VALIDATION_ENABLED=false`: No automatic redirect, allows repeat submissions
- When `NEXT_PUBLIC_COOKIE_VALIDATION_ENABLED=true`: Normal redirect behavior preserved

This enables production testing with UTM parameters without automatic redirects.
