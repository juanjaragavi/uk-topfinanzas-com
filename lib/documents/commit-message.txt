refactor(localization): Update metadata and links for UK market

- Update `app/financial-solutions/metadata.ts` with UK-specific titles, descriptions, keywords, and image references.
- Change "APPLY NOW" link in `lib/navigation/headerNavigation.ts` to point to a UK product requirements page (Barclaycard Avios Plus).
- Update example text in `components/ui/sidebar-example.tsx` to reference a UK product instead of BBVA.
- Remove outdated Mexico/BBVA references from active code components.
