fix: Add missing addService to ad slot definitions

- Added the .addService(window.googletag.pubads()) call to both ad slot definitions in GPTScriptManager.
- This ensures that the ad slots are correctly associated with the pubads service, allowing ads to be fetched and displayed.
