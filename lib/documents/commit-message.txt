feat(seo): replace static sitemaps with dynamic generation

- Removed malformed `public/sitemap.xml` and `app/sitemap.xml` that caused XML parse errors.
- Implemented `app/sitemap.ts` using Next.js `MetadataRoute.Sitemap` API.
- Programmatically traverse blog categories at build time via `fs` for dynamically discovering content routes.
- Addressed TypeScript typing errors related to `changeFrequency` and mapped object types.
